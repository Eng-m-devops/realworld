- name: Deploy RealWorld Application
  hosts: webservers
  become: yes
  vars:
    app_dir: /home/ubuntu/app

  tasks:
    - name: Clean up unused Docker cache and apt cache to free disk space
      shell: |
        docker system prune -f || true
        apt-get clean || true
      ignore_errors: yes

    # -----------------------------------------------
    # 1. إيقاف الحاويات القديمة لتحرير المنفذ 80 لـ Nginx الرئيسي
    # -----------------------------------------------
    - name: Create application directory
      file:
        path: "{{ app_dir }}"
        state: directory
        owner: ubuntu
        group: ubuntu

    - name: Copy docker-compose.yml to server
      copy:
        src: ./docker-compose.Prod.yml
        dest: "{{ app_dir }}/docker-compose.yml"
        owner: ubuntu
        group: ubuntu
        mode: "0644"

    - name: Stop old docker containers to release port 80
      shell: |
        docker compose down --remove-orphans || true
      args:
        chdir: "{{ app_dir }}"
      ignore_errors: yes

    # -----------------------------------------------
    # 2. تثبيت وضبط Nginx على السيرفر الرئيسي
    # -----------------------------------------------
    - name: Install Nginx web server and Certbot
      apt:
        name:
          - nginx
          - certbot
          - python3-certbot-nginx
        state: present
        update_cache: yes

    - name: Copy Nginx site configuration to host
      copy:
        src: ./nginx-host.conf
        dest: /etc/nginx/sites-available/default
        owner: root
        group: root
        mode: "0644"

    - name: Ensure Nginx service is running and enabled
      service:
        name: nginx
        state: restarted
        enabled: yes

    # -----------------------------------------------
    # 3. إنشاء البيئة وتشغيل الحاويات الجديدة
    # -----------------------------------------------
    - name: Create .env file on remote server from secure variables
      copy:
        content: |
          POSTGRES_PASSWORD={{ postgres_password }}
          DJANGO_SECRET_KEY={{ django_secret_key }}
        dest: "{{ app_dir }}/.env"
        owner: ubuntu
        group: ubuntu
        mode: "0600"

    - name: Pull latest docker images and restart containers
      shell: |
        docker compose pull && docker compose up -d
      args:
        chdir: "{{ app_dir }}"
