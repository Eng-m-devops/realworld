server {
    listen 80;
    server_name _;

    client_max_body_size 20M;

    # تقديم صفحات تطبيق React
    location / {
        root /usr/share/nginx/html;
        index index.html index.htm;
        try_files $uri $uri/ /index.html;
    }
}